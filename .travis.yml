language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo add-apt-repository -y ppa:canonical-qt5-edgers/qt5-proper
  - sudo add-apt-repository -y ppa:ubuntu-sdk-team/ppa
  - sudo apt-get update
  - sudo apt-get install -qq ubuntu-sdk cmake
script:
  - cmake .
  - make
  - make test
